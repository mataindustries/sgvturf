---
interface Props {
  eyebrow?: string;
  title: string;
  text: string;
  primaryHref: string;
  primaryLabel: string;
  primaryAnalyticsEvent?: 'primary_cta_click' | 'contractor_cta_click';
  secondaryHref?: string;
  secondaryLabel?: string;
}

const {
  eyebrow = 'Next Step',
  title,
  text,
  primaryHref,
  primaryLabel,
  primaryAnalyticsEvent,
  secondaryHref,
  secondaryLabel,
} = Astro.props;
---

<section class="surface-section surface-section--white">
  <div class="section-shell">
    <div class="collection-cta">
      <div>
        <p class="section-eyebrow">{eyebrow}</p>
        <h2 class="collection-cta__title">{title}</h2>
        <p class="collection-cta__text">{text}</p>
      </div>

      <div class="collection-cta__actions">
        <a class="button button--primary" href={primaryHref} data-analytics-event={primaryAnalyticsEvent}>{primaryLabel}</a>
        {secondaryHref && secondaryLabel && (
          <a class="button button--ghost" href={secondaryHref}>{secondaryLabel}</a>
        )}
      </div>
    </div>
  </div>
</section>
